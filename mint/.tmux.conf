run-shell 'wrapper=$(readlink -f "$HOME/.tmux.conf" 2>/dev/null || printf "%s\n" "$HOME/.tmux.conf"); repo_root=$(CDPATH= cd -- "$(dirname -- "$wrapper")/.." && pwd); tmux source-file "$repo_root/.tmux.conf"'

# Mint-specific tmux config goes here.
set-option -g default-shell /usr/bin/zsh
