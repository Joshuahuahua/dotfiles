# Project memory
- For /home/josh/development/personal/devopsPrinter: when changes affect package behavior, commands, configuration, or printer flows, update README.md in the same change. _(saved 2026-07-21)_

## Project memory
- For /home/josh/development/personal/devopsPrinter: a preview-mode smoke test plan lives at TEST_PLAN.md in the project root. When the user asks to test the printer system, follow TEST_PLAN.md — it covers the CLI note receipt, CLI devops ticket (use work item id 20020), the notification listener (NOTIF_PREVIEW=true + notify-send), and the webserver (/health and POST /print-ticket with preview:true), plus clean-slate/teardown steps. All tests are preview-only (never print to the physical printer). _(saved 2026-07-24)_

## Project memory
- Laptop Bluetooth issue RESOLVED (clean workaround, not root-cause fix): ASUS Vivobook K6602ZC (Linux Mint 22.1 Cinnamon, Ubuntu noble base), MediaTek MT7922 combo Wi-Fi/BT card (btusb/btmtk). Kernel 6.8.0-139-generic had a confirmed Bluetooth regression: NULL pointer dereference during btmtk firmware setup (btmtk_setup_firmware_79xx -> btmtk_usb_hci_wmt_sync -> usb_autopm_get_interface -> __pm_runtime_resume), hci0 never finished init. 6.8.0-138-generic confirmed working (adapter initializes cleanly, real BD address, headphones reconnect fine). Tracked upstream: Launchpad bug #2166561 ("6.8.0-139: btmtk NULL deref kills MT7922 Bluetooth and hangs resume") plus duplicates #2166487/#2166497/#2166509/#2166544/#2166569/#2166577 — all status "New" as of 2026-09-07, no fix yet. Note: Update Manager's other kernel series (6.11, 6.14, 6.17) are 'end of life' (no security updates) so were not used as alternatives; 7.0 is supported but untried since 138 already works.
  - First tried pinning GRUB_DEFAULT to a specific 6.8.0-138-generic advanced-options menu entry ID + `sudo update-grub` — this did NOT reliably take effect (system booted 139 again on next reboot despite grub.cfg showing the correct default; root cause not fully diagnosed, possibly recordfail/hidden-timeout interaction).
  - FINAL FIX: booted 6.8.0-138-generic manually via GRUB menu (hold Shift/Esc at boot to reveal the hidden menu), then used Linux Mint's Update Manager GUI (View > Linux Kernels) to fully uninstall linux-image-6.8.0-139-generic (+ modules/modules-extra). Confirmed via `dpkg -l | grep linux-image` and `/boot/vmlinuz-*` that 139 is gone and 138 is now the newest installed kernel. Also reset `/etc/default/grub` GRUB_DEFAULT back to `0` (removed the now-unneeded custom override) + `sudo update-grub`, since with 139 gone GRUB naturally boots the newest remaining kernel (138) with no fragile default-ID matching required. System is now in a clean, simple state.
  - GOTCHA: simply removing linux-image-6.8.0-139-generic via Update Manager was NOT sufficient - it came back after a reboot. Root cause: the linux-generic/linux-image-generic/linux-headers-generic META-PACKAGES were still installed and always track "whatever is latest in the repo" (139 was still the only/latest candidate; no 138-targeting meta-package version remains in noble-updates). An automatic update check reinstalled linux-generic (a non-automatic top-level install per /var/log/apt/history.log), which pulled 139 back in as a dependency.
  - REAL FIX: purged 139 (linux-image/-modules/-modules-extra/-headers/-tools, all variants) AND the three generic meta-packages (linux-generic, linux-image-generic, linux-headers-generic) while booted into 138, then added a permanent apt pin at `/etc/apt/preferences.d/block-kernel-139` (Pin-Priority: -1) covering all of the above package names - this blocks apt from ever reinstalling any of them again (confirmed via `apt-cache policy` showing Candidate: (none)), even via automatic background update checks. Also reset `/etc/default/grub` GRUB_DEFAULT back to `0` (no override needed) + `sudo update-grub`. System is now in a clean, simple, self-defending state.
  - Todo #7 tracks checking for an eventual upstream fix (via `apt list --upgradable | grep linux-image` for a kernel newer than 139, and Launchpad bug 2166561 status). TO REVERT once fixed: delete `/etc/apt/preferences.d/block-kernel-139`, then `sudo apt install linux-generic` (pulls in the new fixed kernel + reinstalls the generic meta-packages normally). _(saved 2026-09-07)_
