# Project memory
- The repo at /home/josh/development/work/huddler/hub/ (Azure DevOps repo "Huddler - Product", org "diversus") is checked out as multiple git worktrees: workspace1, workspace2, workspace3 (siblings under .../hub/). Each worktree is normally on a different branch at the same time (e.g. workspace1=integration, workspace2=a feature/PoC branch, workspace3=another feature branch) so Josh can work on several branches in parallel without stashing. When helping with this repo, check which worktree/cwd is active to know which branch/PR is in play, and don't assume all three are on the same branch. _(saved 2026-08-05)_

## Project memory
- When doing Azure IaC/subscription-manager work, always ask for explicit confirmation before running any Azure CLI commands that make changes (e.g. az role assignment create, resource creates/deletes) - not just deploys. Read-only/query commands are fine without asking each time, but mutating commands need a go-ahead first. _(saved 2026-08-25)_

## Project memory
- Apps/Sharepoint/src/shared/telemetry/constants.ts has APPLICATION_INSIGHTS_ENDPOINT hardcoded to the REAL PRODUCTION HuddlerTelemetry App Insights component's connection string (InstrumentationKey=c0a4d7df-f834-4e94-98ea-b38325d32f4f, appId e1bafea2-a820-4a9b-9381-8d0ce8153752, in the HuddlerSubscriptionManager RG on the jhollander subscription). This is NOT environment-specific - there's no staging/dev equivalent wired up, so any code path that calls trackTelemetry/recordMetric always sends real events into production telemetry regardless of which environment the SharePoint app is actually deployed to. This is the same class of hardcoded-secret/endpoint issue found repeatedly during the SubscriptionManager Bicep migration work. Not yet fixed - flagged as a known issue to potentially address later (e.g. make it configurable per environment like the other secrets fixed in that migration). _(saved 2026-08-25)_
